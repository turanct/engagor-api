sudo: false

language: php

php:
    - 5.6
    - 7.0
    - 7.1
    - 7.2

before_script:
    - composer install

script:
    - bin/phpcs --standard=psr2 src/
    - bin/phpmd src/ text cleancode,controversial,design,unusedcode
    - bin/phpunit
